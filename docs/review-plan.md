# 종합점검 계획 (2026-09-06 작성)

목적 = 기능완성도·코드최적화·문서정리 3축을 한 번에 정리하기 위한 실행 계획.
범위 확정은 사용자 승인(질문창, 4개 축 전부 선택) 기준 — 아래 5축 전부 포함.

⚠ 이 문서는 계획만 담음(전역 규칙 12 — 계획 요청 시 실행 안 함). 각 축 실행은
축별로 별도 승인 후 진행.

## 우선순위 — 축1부터 (추천)

코드 구조·문서정리는 되돌리기 쉬움. 반면 축1(미검증 백로그)은 "되는지 모르는 채로
기능이 계속 쌓이는" 유일한 시간누적형 리스크임 — 먼저 처리.

## 축1: 실기기 미검증 백로그 소거 (최우선)

**문제**: 라운드2~19 거의 전 라운드가 "남은 것: 실기기 확인 필요"로 끝남. 다음 라운드
서두의 "✓ 확인 완료" 문구가 그 항목들을 해소하기도 하지만 **선택적으로만 언급**돼
있어(형광펜·스와이프처럼 자주 언급되는 것 위주), 언급 안 된 항목은 실제로 확인됐는지
문서만 봐서는 알 수 없음. 아래 표가 이 갭을 처음으로 추적한 것 — grep으로 뽑았고
"추정" 칸은 검증 안 된 추론.

⚠ **실제로 찾은 구체 사례** — 라운드8의 상태바 `clip-path` 수정("상태바 clip이 실제로
텍스트를 잘라내는지")과 하단바 오탭 여부는, 라운드9 서두의 "✓ 라운드8 확인된 것" 목록에
**둘 다 빠져 있음**(그 목록엔 제목메뉴·형광펜·하단바 스와이프차단만 있음 — `CLAUDE.md:729`
"상태바·풀다운 중 풀다운은 이걸로 확인 끝남"이 상태바는 여전히 미확인이라는 뜻으로 읽힘).
이후 라운드에서도 상태바 관련 재확인 문구를 못 찾음 — **라운드8부터 지금까지 상태바가
실기기에서 실제로 잘리는지 한 번도 명시적으로 재확인된 적이 없을 가능성**(추정, 반증:
사용자가 실기기로 상단 텍스트가 상태바에 안 가려지는지 직접 봐서 확인하면 됨).

| 라운드 | 날짜 | 미확인 항목(원문 요약) | 추적 메모(추정) |
|---|---|---|---|
| 19 | 09-06 | ~~트래시 무채색·스크러버 코랄 시인성~~(✓09-06 해결·보라로 재배치·기기 확인), ~~폰트 스와치 스크롤 손맛~~(✓09-06 한 줄 축소로 재작업·기기 확인), 초기화 팝업 UI, ~~`1-1-N` 잘림~~(✓09-06 구조적으로 종결) | 남은 건 초기화 확인 팝업 문구·버튼 크기 하나 |
| 18 | 09-06 | ~~스크러버 점 상태색 시인성~~(✓09-06, 트래시가 보라로 바뀌며 해결), 아코디언 터치크기, 종합시험 100문제 완주체감, 버튼배타 손맛 | 나머지 3개 미확인 |
| 17 | 09-06 | 버튼 테두리 시인성·토글손맛, 학사모 아이콘, ~~형광펜 "선택 먼저" 경로~~(✓09-06 코드 자체검증으로 종결 — 버그 아님, 위 표 6번 참조) | 버튼 테두리·학사모 아이콘 2개 미확인 |
| 16 후속2 | 09-06 | 핀치 체감, 배지 가독성, 바 순서 손맛 | 미확인 |
| 16 | 09-06 | 핀치줌 손맛, 상태칩 가독성, n/nn 전환 체감 | 상태칩은 이후 배지로 대체돼 무관, 핀치줌은 후속2서 매끄럽게 재작업(재확인 필요), n/nn은 이후 여러 번 재설계돼 최신 UI로 재확인해야 함 |
| 15 후속2 | 09-06 | 폰트 스와치 터치크기, 시험아이콘 구분감, 필터버튼 테두리글로우 | 필터버튼 테두리는 라운드16후속서 제거돼 무관(폐기됨), 나머지 미확인 |
| 15 | 09-06 | 폰트슬라이더 드래그감, 목록번호 가독성(챕터100개), 트로피배지 크기 | 폰트슬라이더는 이후에도 계속 손댐(라운드19까지) — 최신본 기준 재확인 필요 |
| 14 | 09-06 | 폰트팝업 가독성, 상시버튼 3개 터치간격, 보관카드뷰 롱프레스 손맛 | 폰트팝업 위치·상시버튼 자체가 라운드15·17서 재설계됨 — 최신본 기준 재확인 |
| 13 | 09-06 | 검색 키보드 오버레이, 챕터행 압축 손맛, 복습카드뷰 스와이프·롱프레스 | 미확인 |
| 12 (+후속2) | 09-06 | 시트높이·챕터행레이아웃·슬라이더조작감·보관복구 손맛 | Notion 라벨 반영 자체는 "완료로 간주"(문서 명시) — UI 손맛은 미확인 |
| 10 | 09-05 | iOS 키보드+상태바 clip 상호작용, 롱프레스 타이밍 손맛 | 미확인 |
| 8 | 09-05 | ~~상태바 clip 실제 차단 여부~~(✓09-06 기기 확인 — 잘림), ~~하단바 오탭 여부~~(✓09-06 기기 확인 — 문제 없음) | **종결** — 가장 오래 방치됐던 항목, 이번에 해소 |
| 6 | 09-05 | 스와이프다운 손맛, 홈인디케이터 간격, 프리페치 체감속도 | 미확인 |
| 4 | 09-05 | 챕터행 전체탭 영역의 스크롤중 오탭 여부 | 챕터행 자체가 이후 아코디언(라운드18)으로 재설계 — 최신본 기준 재확인 |
| 2~3, 5, 7, 9 | 09-05 | (각 라운드 본문 참고) | 다음 라운드 서두에서 대부분 명시적으로 "확인 완료" 처리됨 — 표에서 생략 |

**절차**:
1. 위 표를 실제 체크리스트로 옮겨(예: `.claude/handoff/` 아님, 이 문서 자체를 갱신) 사용자가
   실기기로 하나씩 확인
2. 확인 결과를 이 표에 바로 기록(✓ 확인됨 / ✗ 재현됨(→ 새 라운드로 수정) / 해당없음(설계
   변경으로 무관해짐))
3. 종료 기준: 표의 모든 행이 ✓ 또는 "해당없음"으로 종결

## 자체검증 1차 결과 (2026-09-06, 로컬 `wrangler dev` + Playwright)

🖍 **자체확인** — 실기기로 넘기기 전 로컬에서 판단 가능한 항목부터 직접 렌더·조작해 확인함.
결과를 아래 6개 항목에 반영, 순수 기기 손맛만 남은 항목만 표시.

| # | 항목 | 결과 | 남은 것(기기 전용) |
|---|---|---|---|
| 1 | 라운드8 상태바 `clip-path` | ✓✓ **실기기 확인 완료** — 상태바 아래서 깔끔하게 잘림. **종결.** | 없음 |
| 2 | 라운드8 하단바 오탭 | ✓✓ **실기기 확인 완료** — 문제 없음. **종결.** | 없음 |
| 3 | 라운드19 트래시/스크러버 색 | ✓✓ **실기기 확인 완료 — 종결.** 무채색은 기본 미확인 회색과 구분 안 됨(사용자 실측) → 보라(`--trash:#B98FFF`)로 재배치, 배포(`Version ID ff25df57`) 후 재확인 "잘보임". 남색 대신 보라를 택한 이유: 기존 완전히앎(`--mastered` 파랑)과 계열이 겹치지 않게 | 없음 |
| 4 | 라운드19 폰트 6종 스크롤 | ✓✓ **실기기 확인 완료 — 종결.** Aa/가나 미리보기 제거, 폰트명 한 줄만 그 폰트로 렌더(카드 높이 축소로 팝업 뒤 본문이 더 보임), 배포 후 재확인 "잘보임" | 없음 |
| 5 | 라운드19 `1-1-N` 칩 잘림 | ✓ 실제 최댓값(14-6-10)보다 훨씬 큰 극단값(`14-10-100`)으로 테스트해도 안 잘리고 옆 아이콘과 안 겹침 — 칩이 `width:auto`라 내용에 맞춰 늘어나는 구조 | 없음(구조적으로 잘릴 수 없음 — 이 항목은 종결) |
| 6 | 라운드17 형광펜 "선택 먼저" | ✓ **종결 — 코드 정상, 버그 아님(직접증거로 원인 확정).** 동일 JS 턴 안에서 선택→즉시 클릭 시 정상 복원·적용됨. 도구 호출을 분리하면(라운드트립 발생) 실패하길래 `performance.now()`로 간격을 직접 측정 — **3,752ms**로 앱 자체의 의도된 신선도 창(`Date.now()-lastSelAt<2000`, `index.html:922`)을 넘겨서 조건이 거짓이 된 것. 실제 사람의 "선택→탭" 간격은 2초 안쪽이 정상이라 이 실패는 테스트 도구의 라운드트립 지연이 만든 아티팩트일 뿐, 앱 로직과 무관 | 없음 — 원인이 코드가 아니라 테스트 방법 자체였음이 측정으로 확정됐으므로 재확인 불요 |

축3(상단바 고정영역화 포함 여부)은 이번 라운드 범위에서 제외 — 라운드16에서 "의견 제시만" 상태로 남아 있고 별도 버그·불편 보고가 없어 축1 완료 후 재검토 권장.

## 축2: 코드 구조 점검

**실측**: `src/worker.js` 1,151줄, `public/index.html` 2,406줄 — 둘 다 분리 없는 단일 파일.
**규칙8(구조 걸림) 트리거 실측 결과**: 19라운드 커밋 로그·CLAUDE.md 어디에도 "편집 위치를
못 찾음"·"같은 파일 반복 버그"·"격리 테스트 불가" 보고가 없음 — 즉 지금까지 실제로 이
크기 때문에 막힌 적은 없었음(추정 아님, 문서 전수 검색 결과).

**판단**: 트리거가 없으므로 파일 분리(리팩터링)는 이번 점검에서 보류 추천 — 전역 규칙
"필요할 때만 정리, 미리 판단하지 않는다"와 일치. 대신 저비용으로 할 수 있는 것만:
- dead code 스캔(안 쓰는 함수·CSS 클래스·구버전 KV 키 참조) — 위험도 낮음
- `CLAUDE.md`에 이미 기록된 "역사적으로 폐기된 코드"(예: `grades`/`daily:v1:*` 관련 로직
  잔재)가 실제 파일에 남아 있는지 grep으로 재확인

## 축3: 기능 완성도 — 미구현 1건

라운드16에서 "의견만 제시하고 보류"한 항목 1건만 유효: **상단바를 하단바처럼 고정
영역화**(본문이 상단 칩 아래부터 스크롤되게). Claude 의견은 이미 CLAUDE.md 라운드16
섹션에 기록됨(추천안: 상단 전체에 옅은 배경 깔기).

**결정 필요**: 이번 종합점검에 포함할지 — 포함 시 축1과 별도로 승인 후 구현.

## 축4: 문서 정리

**실측**: `CLAUDE.md` 1,837줄. 최상단부터 라운드19~라운드2까지의 "현재 상태" 로그가
32~1233줄(≈1,200줄, 전체의 65%)을 차지. 1234줄 이후는 이미 "역사 기록(대체됨)"으로
문서 자체가 표시해 둠.

**제안**: 축1(백로그 소거)이 끝난 뒤에 진행 — 어떤 라운드가 "완전히 종결"됐는지가 축1
결과로 확정돼야 아카이브 경계를 정확히 그을 수 있음. 안 그러면 아직 미확인인 항목을
아카이브에 묻어버릴 위험.
- 안: 라운드2~11(초기 프로토타입~제목편집 확정까지, 대부분 이후 라운드에서 UI 자체가
  재설계됨)을 `docs/history-round2-11.md`로 이동, `CLAUDE.md`엔 요약 3~5줄만 남김
- 최신 라운드(12~19)는 그대로 유지 — 아직 참조 빈도가 높음
- 최상단 "여기부터 읽을 것" 온보딩 규약은 유지해야 함(신규 세션이 이 문서로 부트스트랩)

## 축5: 상류(Notion/KV) 청소

**실측**: 옛 KV 키 `grades`·`daily:v1:*`·`schema:v1`이 프로덕션에 남아 있음 — CLAUDE.md가
이미 "새 코드가 안 읽어 무해"라고 명시. 작업량 작음(독립적으로 아무 때나 가능).

**결정 필요**: 정말 지울지(`wrangler kv key list` → `delete`) — 무해하다면 지울 이유도
약해서, 사용자 취향의 문제로 봄.

## 실행 순서 제안

1. **축1** — 실기기 검증 세션(사용자 주도, Claude는 체크리스트 갱신·발견된 버그 즉시 수정 지원)
2. 축1 진행 중 발견되는 실제 버그는 즉시 그때그때 라운드로 수정(기존 작업 방식 그대로)
3. **축4** — 축1 종료 후, 종결된 라운드 범위를 확정해 아카이브
4. **축5** — 독립적, 아무 때나(5분 작업)
5. **축2** — 보류 추천(트리거 없음), 축1 진행 중 실제로 편집이 막히는 경험이 나오면 그때 착수
6. **축3** — 포함 여부 사용자 결정 대기
