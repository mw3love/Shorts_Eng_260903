# CLAUDE.md — Shorts Deck

노션에 쌓인 영어 표현을 유튜브 쇼츠처럼 한손 스와이프로 복습하는 웹앱.
이 파일은 git으로 따라다니므로 어느 PC에서 작업하든 동일하게 적용된다.

## 무엇을 푸는 프로젝트인가

크롬 확장 `youtube_dual_subtitle`이 유튜브 자막에서 고른 영어 표현 + AI 해설을
Notion DB `3. 영어 DB`에 자동 저장한다. **저장은 되는데 복습이 안 된다** —
2026-09-03 실측 시점에 **1,323행**이 쌓여 있고 표본 전부 `복습 횟수 = 0`,
`상태 = 비어 있음`이었다. Notion 안에도 `0차 완료`·`1차 완료` 뷰가 이미 있지만
한 번도 안 돌았다. 도구가 없어서가 아니라 **Notion에서 복습하는 게 재미없어서**
멈춘 것으로 본다.

⚠ **행 수를 공개 페이지(`*.notion.site`)에서 세지 말 것.** 표 뷰가 lazy-load라
스크롤 전까지 21행만 렌더한다 — 초기에 이걸 전수로 오독해 "21장짜리 작은 DB"라는
잘못된 전제로 설계할 뻔했다. 정확한 규모는 API의 `databases/{id}/query` 페이지네이션
결과로만 판단한다.

그래서 목표는 "복습 기능"이 아니라 **조작감**이다. 참고 대상: Slidebox(한손 스와이프),
유튜브 쇼츠(무마찰 진행), 클래시 로얄(가변 보상).

## 현재 상태 (2026-09-04 갱신) — 여기부터 읽을 것

**배포된 것**(2026-09-03): 저장 개편(1,323/1,323 동기화 완주, 채점이 Notion에 반영되는
것까지 확인) + 그날 재작성한 구버전 UI(1단/2단 카드 모델, `Version ID
0286c237-ed3c-4bf5-bcd3-fd91ab0d31a6`).

**그 이후 UX를 통째로 다시 설계 중** — `prototype.html`(Artifact 목업)에서 여러 라운드에
걸쳐 확정했고(가장 최근: 스와이프 커밋 로직 재설계·프리뷰 밝기 통일, 2026-09-04), 1단(카드)
완전 폐지 등 핵심 모델이 바뀌었다. **아직 `public/index.html`
(실제 배포 앱)엔 하나도 반영 안 됨** — 그래서 지금 실사용자에게 보이는 화면은 위 문단의
구버전 그대로다. 새 설계 전체 내용은 아래 `UX 규약` 섹션(전면 교체 — 더 이상 구버전 설명
아님).

✓ **실기기 검증 완료(2026-09-04)** — 사용자가 폰으로 직접 써보고 "사용측면에서는 크게
문제 없음"으로 확인(롱프레스 진행 링·시트 스와이프 닫기·완료 펄스·세션종료카드·스와이프
프리뷰 전반, 항목별 세부 코멘트는 없었음 — 전반적 통과로 이해).

⚠ **`npx wrangler deploy`는 반드시 프로젝트 폴더 안에서 실행할 것.** 홈 디렉토리
(`C:\Users\7make`)에서 실행하면 `wrangler.toml`을 못 찾아 "autoconfig"가 현재 폴더
전체를 훑다가 Windows의 레거시 호환 정션(`Application Data`→`AppData\Roaming`)
재귀 탐색에서 `EPERM: scandir` 오류로 죽는다(2026-09-03 실측, 정션 자체는 정상 —
엉뚱한 위치에서 실행한 부작용). 프로젝트 폴더로 `cd` 후 실행하면 바로 통과한다.

✓ 실기기 검증(2026-09-04, 사용측면 문제없음 확인) · ✓ 하단 시트 디자인(캘린더+슬라이더,
`design-bakeoff` 라운드1~3 확정 후 `prototype.html` 반영 완료 — 확정 토큰은
`~/.claude/design-system/projects/shorts-deck.md` 참고) 둘 다 끝남.

남은 것: `public/index.html`에 전체 이식 + `wrangler deploy`.

### 해결된 것 — 다시 밟지 말 것

**옛 `503` 스톨(1,101/1,323에서 6/6 실패)의 원인은 상태 값 비대가 맞았다.**
`sync:state`에 누적 카드를 통째로 담아 약 1.7MB까지 커졌고, 매 단계 그걸 파싱·직렬화하다
무료 요금제 CPU 10ms를 넘겼다. 인덱스+버킷으로 쪼개 값 크기를 유계로 만든 뒤 **같은
지점을 그냥 통과해 1,323까지 갔다** — 가설이 반증되지 않고 확인된 것.

**카드 본문은 이제 Notion 원본 블록으로 보관한다**(평문 문자열 배열 폐기). 렌더는 표시
시점에 클라이언트가 한다 — 그래서 **앞으로 렌더링을 몇 번 바꾸든 재동기화가 필요 없다.**

⚠ 카드별 개별 KV 저장은 여전히 불가 — 무료 **KV 쓰기 1,000회/일** 제한에 1,323장이 걸린다.
⚠ 파서를 바꾸면 카드의 `parserVersion`을 올릴 것. 증분 로직이 그 값이 다르면 재사용하지
않고 다시 긁는다(현재 `2`). 이게 없으면 옛 포맷이 영원히 살아남는다.

## 상류 데이터 — Notion DB 실측 (2026-09-03)

DB: `3. 영어 DB` (`277763872455804ebc00cb991e8e3247`, 워크스페이스 `mw2love`)

| 속성 | 타입 | 쓰임 |
|---|---|---|
| 이름 | title | 카드 앞면(영어 문장). 자막 문장 우선 |
| URL | url | 원본 유튜브 링크 — 카드에서 바로 점프 |
| 복습 횟수 | number | **본 총 횟수**(단조 증가). 차수와 별개 축 |
| 상태 | select | **체감 난이도** — 쉬움 / 적당함 / 어려움 / 완료 |
| 완료도 | select | **진도(차수)** — `0` / `1차 완료` / `2차 완료` / `3차 완료` |
| 날짜 / 생성 일시 | date / created_time | 저장 시각 |
| 복습 완료 · 적당함 | button | ⚠ API로 쓸 수 없음 — 무시한다 |

⚠ **`상태`와 `완료도`는 이름이 비슷하지만 다른 축이다.** 채점의 체감 난이도는
`상태`로, 복습을 몇 차 돌았는지는 `완료도`로 간다. 그리고 **둘 다 `select` 타입이라
타입으로는 구분이 불가능하다**(2026-09-03 사용자가 `완료도`를 status→select로 변경).
그래서 `discoverSchema()`는 타입이 아니라 **옵션 내용**으로 역할을 판별한다 —
`쉬움|어려`가 있으면 난이도, `N차 완료`가 있으면 진도. 첫 구현은 타입으로 골랐다가
진도 컬럼을 집어 채점이 조용히 유실됐다.

⚠ **판별 결과(`schema:v1`)를 KV에 캐시하는데, 이게 낡으면 채점이 또 조용히 유실된다.**
`writeGrade()`는 이 캐시의 `progressType`을 그대로 믿고 `{select:...}`냐 `{status:...}`냐를
정한다. 2026-09-03 실측: 사용자가 `완료도`를 status→select로 바꾼 뒤에도 KV엔 옛 status
스키마가 남아 있어 PATCH가 검증에 걸렸고, `writeGrade`가 예외를 삼키는 구조라 앱은 아무
일 없다는 듯 넘어갔다(응답의 `notionOk:false`가 유일한 신호였고 프런트는 그걸 안 본다).
→ `syncStep()`이 새 동기화를 시작할 때마다 `schema:v1`을 다시 쓴다. **그 한 줄을 지우지
말 것.** DB 스키마를 손으로 바꿨으면 `/api/sync?restart=1`로 캐시를 갱신하는 게 확실하다.

⚠ `완료도`의 첫 옵션은 `0차`가 아니라 그냥 **`0`** 이다. 화면에 "0차"로 보이려면
앱에서 라벨링한다. 빈 값도 `0`과 같게 취급한다.

⚠ `적당함`은 값이 아니라 **버튼 속성 이름**이다. 표 뷰에서 모든 행에 "적당함"이 찍혀
보이는 건 버튼 라벨이지 데이터가 아니다(초기에 이걸 데이터로 오해했다).

⚠ **속성 이름을 하드코딩하지 말 것.** 위 판별은 전부 타입 + 옵션 내용 기반이다.
크롬 확장도 같은 전략을 쓴다(사용자가 컬럼 이름을 바꿔도 안 깨짐).

### 카드 뒷면은 DB 컬럼이 아니라 "페이지 본문"에 있다

```
페이지 본문
├── 자막 문맥 (21장 중 12장)
├── 「영어표현 + 한국어 뜻」 한 줄     ← 1단계 힌트. 21/21 모든 카드에 존재
├── 해설 문단 (구성·의미·관통하는 이미지)
└── 표 (어원/예시)                    ← 2단계 전체 해설
```

이 구조 덕에 `탭 1회 = 뜻만` / `탭 2회 = 전체 해설`이 성립한다. 파싱은
`src/worker.js`의 `splitBody()`. 그래서 카드 하나를 만들려면 **행 조회 + 본문 조회 2회**가
필요하고, Notion은 평균 3 req/s라 실시간 조회가 불가능 → KV 캐시가 선택이 아니라 필수.

## 아키텍처 — 왜 이 모양인가

**Cloudflare Worker 하나가 앱과 API를 같은 출처에서 서빙한다.**

⚠ **`api.notion.com`은 `Access-Control-Allow-Origin`을 보내지 않는다**(2026-09-03 curl 실측).
브라우저 페이지는 Notion API를 직접 못 부른다. 크롬 확장이 되는 건 `host_permissions`로
CORS를 면제받기 때문이지 Notion이 허용해서가 아니다. 게다가 토큰을 프런트에 두면 털린다.
→ 순수 정적 호스팅(GitHub Pages 등)만으로는 이 앱이 성립하지 않는다.

**기각한 대안:** GitHub Pages + Actions. 무료 개인 계정은 private repo로 Pages 호스팅이
아예 불가하고, 유료로 올려도 게시된 사이트는 기본 public이다. 게다가 grade 쓰기 경로가
없어 결국 Worker를 또 붙여야 한다 — 컴포넌트 2개보다 1개가 낫다.

### ⚠ 무료 요금제는 요청당 서브요청 50회 — 그래서 동기화가 청크로 쪼개져 있다

카드 하나를 만들려면 본문 블록 조회가 1~2회(표가 있으면 2회) 필요하다. 21장만 해도
`스키마 1 + 행 조회 1 + 본문 21 + 표 21 ≈ 60회`로 상한을 넘는다. 첫 구현이 이걸 한
요청에서 다 하려다 응답 없이 멈췄다.

**"표 조회를 빼서 아끼자"는 해법이 아니다** — 이 프로젝트의 전제가 "카드가 계속 쌓인다"라
절약형 최적화는 시간만 벌고 언젠가 반드시 다시 터진다. 그래서:

- `syncStep()`이 **서브요청 예산(`SUB_BUDGET = 40`)만큼만** 처리하고 진행 커서를
  KV `sync:state`에 남긴다. 버킷이 50장 차면 즉시 flush하므로 상태 값은 항상 유계다.
- cron(`*/5 * * * *`)이 알아서 이어 돌린다. 진행 중이면 계속, 끝났고 인덱스가 6시간보다
  낡았으면 새로 시작.
- `GET /api/cards`는 **Notion을 부르지 않는다** — 인덱스에서 뽑고 해당 버킷만 읽는다.
  아직 인덱스가 없으면 `202 + syncing:true`를 주고, 앱은 씨앗으로 퇴화한다.
- `GET /api/sync?restart=1`로 수동 강제 재동기화.

⚠ **KV의 `get`/`put`/`list`/`delete`도 이 50회에 포함된다**(2026-09-03 공식 문서 확인).
`fetch`만 세면 안 된다 — 버킷을 여러 개 읽는 경로에서 특히 위험하다. 그래서 동기화
루프의 KV 접근은 `kvGet`/`kvPut`으로 감싸 같은 예산에서 차감한다.

카드 수가 늘어도 cron 틱이 늘 뿐 설계는 그대로다.

### ⚠ 전량을 폰에 내려보내지 않는다

1,323장 본문은 약 2MB다. 매번 받으면 셀룰러 첫 로딩이 느리고 Safari `localStorage`
한도(~5MB)에도 부딪힌다. 그래서 **본문은 필요한 만큼만** 서버에서 골라 준다
(`pickCandidates()`, 뽑기 규칙은 아래 UX 규약 참조). 앱은 8장씩 받아 큐가 3장 아래로
줄면 조용히 더 채우므로, 사용자 입장에선 끝이 없다.

⚠ 단 **인덱스(제목·차수만)는 통째로 내려도 된다** — 약 100KB다. 모아보기 목록 뷰가
`GET /api/index`로 이걸 그대로 쓰고, 카드 하나를 펼칠 때만 `GET /api/card?id=`로 본문을
따로 받는다.

**Artifact를 프로덕션으로 쓸 수 없는 이유:** Artifact의 CSP가 외부 fetch를 차단한다.
`prototype.html`(Artifact로 게시됨)은 데이터를 인라인한 **조작감 테스트용 스냅샷**일 뿐이고,
실제 앱은 `public/index.html`이다. 둘은 별개 파일이며 자동 동기화되지 않는다.

## ⚠ pageId는 안정적인 키가 아니다

크롬 확장은 재저장(형광펜 추가 후 다시 저장)을 **"새 페이지 생성 + 옛 페이지 archive"**로
구현한다(Notion API에 본문 통째 교체가 없어서 — 함정 상세: `~/.claude/wiki/notion-페이지-덮어쓰기-재저장.md`).
그래서 **재저장할 때마다 pageId와 URL이 바뀐다.** 확장 코드 주석은 이걸
"단어장 페이지엔 사실상 무해"라고 적어 뒀는데, **복습 앱이 생기는 순간 그 가정이 깨진다** —
pageId를 키로 학습 이력을 저장하면 재저장 한 번에 그 카드의 이력이 통째로 사라진다.

→ 학습 이력의 키는 **정규화된 제목**(`cardKey()`)이다. pageId는 Notion에 쓸 때만 쓴다.

## 저장 위치 — 무엇을 어디에 두는가

| 데이터 | 위치 | 이유 |
|---|---|---|
| 학습 이력(grades) | Worker KV `grades` | 진실의 원본. 제목 키라 pageId churn에 면역 |
| 복습 횟수 / 상태 / 완료도 | Notion 속성 | best-effort 미러 — 기존 `0차 완료` 뷰가 살아남음 |
| 카드 목록 | Worker KV `index:v2` | 제목·차수·버킷번호만. 약 100KB — 통째로 내려도 됨 |
| 카드 본문 | Worker KV `bucket:v2:N` | 50장 단위. 세션에 필요한 버킷만 읽는다 |
| 스키마 판별 결과 | Worker KV `schema:v1` | ⚠ 낡으면 채점이 조용히 유실 — 동기화 시작마다 갱신 |
| 동기화 커서 | Worker KV `sync:state` | 진행 중일 때만 존재. 끝나면 삭제 |
| 오프라인 캐시 | localStorage | ⚠ 보조일 뿐 — iOS는 영구성을 보장하지 않는다 |

⚠ **iOS PWA의 localStorage/IndexedDB를 진실의 원본으로 삼지 말 것.** 홈 화면 앱은
7일 만료를 면제받지만 Safari 방문기록 삭제·저장공간 부족 시 함께 지워진다.

## UX 규약 (2026-09-04 전면 재설계 — `prototype.html` 목업에서 12라운드 확정, 아직 미배포)

⚠ 이 섹션은 **목업(`prototype.html`)의 설계**를 기술한다. 실제 배포 앱은 위 `현재 상태`가
말하는 구버전(1단/2단) 그대로다 — 이식 전까지는 실사용자에게 적용 안 된다.

### 핵심 변경 — 1단(카드) 완전 폐지

탭 1회면 바로 본문이 보이던 흐름(2026-09-03 "2단 공개 폐기" 결정)을 밀어붙여 아예
**단일 화면**으로 만들었다: 화면 하나 = 제목(항상 최상단, 크게) + 본문(Notion 블록 그대로).
힌트 줄을 따로 뽑아 굵게 강조하던 것도 없앴다 — 그 줄이 항상 "뜻"이라는 보장이 없어서
(사용자 지적. 21/21 표본은 확인됐지만 앞으로도 그러리란 보장은 없음), Notion 원본 서식
(굵게 등)을 그대로 믿는 쪽으로 갔다.

### 제스처

| 제스처 | 동작 |
|---|---|
| 좌우 스와이프 | 이전/다음 카드로 이동(채점 없음). 제목+본문이 카드처럼 같이 슬라이드, 옆 카드의 제목+본문이 실제 밝기 그대로(하단만 페이드) 같이 딸려 들어옴 — 예전엔 옅게(불투명도 .5) 죽였는데 스왑 순간 밝기까지 튀어 보여서 뺐다(2026-09-04 15라운드) |
| 상하 | 네이티브 스크롤 |
| 완료 버튼 탭 | n+1(다음 차수로 승급) |
| 완료 버튼 롱프레스(0.45초, 진행 링 표시) | 0/1/2/3차 미니 팝업으로 직접 텔레포트 |
| 되돌리기 버튼 | 이동+채점 통합 히스토리 스택에서 한 단계씩 되짚음(다단계, 연타로 오늘 한 것 전부 취소 가능) |

⚠ **채점에 "몰랐다/틀렸다" 신호가 없다.** 스와이프로 그냥 넘기면 스킵일 뿐 — 강등
메커니즘 자체를 없앴다(2026-09-04 결정, 옛 3단계 이해/애매/모름 채점·"복습 횟수"에 따른
강등·"다시 만남" 보너스 전부 폐기).

⚠ **되돌리기는 "봤다"는 사실(오늘 카운트)까지는 안 되돌린다** — 채점(차수)만 되돌리고
그 카드를 오늘 열어봤다는 기록은 남는다. 예전(2026-09-03)의 "직전 1건만, 복습 횟수까지
원상복구" 규약은 폐기.

**스와이프 커밋 판정은 고정 px가 아니라 폭 비율+속도 하이브리드다**(2026-09-04, Swiper.js
`longSwipesRatio`/`longSwipesMs`·Material Design 3 duration 토큰 참고해 재설계) — 천천히
끌 땐 화면폭의 50% 이상, 빠르게 튕기면(300ms 안에 18% 이상) 짧은 거리로도 커밋. 옛 고정
px(`TH`) 방식은 화면이 크면 살짝만 끌어도 넘어가버리는 문제가 있어 폐기. 코드는
확인했지만("자체확인") 실기기 체감 검증 전.

⚠ 좌우 스와이프와 Safari 뒤로가기 제스처는 거의 간섭하지 않는다 — iOS 뒤로가기는
**화면 좌측 끝에서 시작**해야 걸리고, 우리 제스처는 중앙에서 시작한다(2026-09-03 확인,
단일화면이 돼도 여전히 유효).

### 화면 구성

- **최상단 줄**(카드와 무관, 드래그해도 안 움직임): 오늘 카운터(중앙 — 오늘 "본" 고유
  카드 수, 채점 여부 무관. 세션 진행 중엔 `n/total`로 바뀜) + 생성일(우측, 차수색 배지).
  우상단은 시선이 잘 안 가서 상단 정보를 여기로 몰았다(2026-09-04 사용자 판단).
- 그 아래 제목(크게, 좌우 드래그 시 본문과 같이 이동) — 항상 최상단 근처 고정은 유지.
- 본문 인라인 코드(사용자가 브라우저 확장으로 친 형광펜) = **코랄 실색 배경 + 어두운
  텍스트**("하이라이터 반전" — 코랄 텍스트를 코랄 배경 위에 얹던 이전 안은 대비가 낮아
  폐기, 실측 대비비로 확인).
- **강조색은 코랄 하나로 통일**(표 헤더, 시트 활성 탭, 슬라이더 손잡이 등). 예전에 쓰던
  gold는 그 의미(진행 게이지·다시 만남 보너스 테두리)가 이번 재설계에서 다 빠져서 폐기.
  차수(0/1/2/3차) 표시만 예외로 회색/파랑/주황/빨강 4색 유지 — 카테고리 4개를 단색으로
  못 나타내서("다른 색이 꼭 필요한" 경우로 판단).
  ⚠ **캘린더 개수 숫자는 예외**(2026-09-04 `design-bakeoff` 라운드3 확정, `prototype.html`
  반영 완료) — 코랄은 "오늘" 칸 테두리(링+글로우)에만 쓰고, 개수 숫자 자체는 밝은 흰색
  (`--paper`, `font-weight:300`)으로 뚜렷하게: 강조색을 "값 표시"가 아니라 "의미 있는
  상태"에만 쓴다는 원칙(`~/.claude/design-system/preferences.md`)과 맞춘 것 — 흰색도
  코랄(강조색)은 아니라서 원칙 자체는 안 깨짐. 칸 모양은 그림자·그러데이션 없는 플랫
  (`border-radius:5px`, `background:var(--ink3)`). 상세: `~/.claude/design-system/
  projects/shorts-deck.md`.
- 완료 성공 시 버튼이 짧게 펄스. 세션(날짜별/목록 복습)이 끝나면 짧은 완료 카드(체크
  아이콘 + "오늘 N개 복습 완료", 0.9초) 후 평소 화면 복귀.
- 2단이 없어졌으므로 글자 크기 조절은 **제목/본문 따로**(하단 시트 최상단 슬라이더),
  값은 `localStorage`에 저장돼 다음 접속에도 유지.

⚠ 채점이 완전히 버튼 전담이라 롱프레스 시 iOS 텍스트 선택 팝업이 뜰 수 있다 — 카드
영역엔 `-webkit-touch-callout:none` 유지, 본문 텍스트는 선택 가능해야 하므로 예외.

⚠ **iOS Safari `position:fixed;inset:0`은 주소창이 떠 있을 때도 큰 뷰포트 기준으로
높이를 잡는 경우가 있다**(2026-09-03 실기기 스샷에서 확인) — `#app`에 `height:100dvh`를
유지할 것. 단일화면 재설계 이후 재확인은 안 함.

### 하단 시트(모아보기) — 우측 레일 ☰로 열림

⚠ **진입점은 이거 하나뿐이다.** 예전엔 "오늘 카운터 꾹 누르면 열림"이라는 숨은 경로가
있었는데, 사용자가 그걸 몰라서 "캘린더가 안 보인다"는 버그로 오인한 적이 있어 없앴다
(2026-09-04).

- 항상 **캘린더가 기본**. 실제 날짜 그리드(일~토 정렬), 각 칸 좌상단에 날짜 숫자,
  중앙에 그날 본 개수(밝은 흰색, 0이면 숫자 자체를 안 그림 — 위 "코랄은 오늘 칸에만"
  참조). 배경 농도로 개수를 또 표현하지 않는다(숫자 하나로 충분하다는 판단). **최근 4주 고정 창** — 그 이전 기록은 캘린더에
  안 뜨고 검색/차수탭으로 찾는다(캘린더=최근 리듬, 검색/탭=전체 아카이브로 역할 분리.
  무한 과거 스크롤은 기각 — 아래 "정통 간격반복 거부" 방향과 같은 이유).
- 날짜(오늘 포함)를 누르면 그 자리가 **그날 본 카드 제목 목록**으로 바뀐다(같은 자리
  토글, `← 캘린더`로 복귀). 오늘 칸은 0개여도 눌러서 빈 상태를 볼 수 있다.
- 검색 + 전체/0/1/2/3차 탭도 같은 자리(`#calArea`)를 공유 — 전체 DB 대상, 탭 재클릭하면
  해제돼 캘린더로 돌아간다.
- **목록에서 아무 항목이나 고르면 그 지점부터 순차 복습 세션이 시작된다**(맨 위=처음부터,
  다른 항목=거기서부터). 캘린더 날짜 클릭도 같은 세션 메커니즘을 쓴다.
- 시트를 아래로 스와이프하면 닫힌다(리스트/캘린더 스크롤이 맨 위일 때만 반응 — 스크롤
  중엔 안 뺏김. 손잡이 영역은 항상 반응).

⚠ 캘린더의 "지난 날짜" 목록·개수는 지금 목업에서 근사치다(메인 덱 앞부분을 재사용) —
실제 이식 시엔 서버가 그날 실제로 본 카드 id 목록을 내려줘야 한다.

### 메인 덱 공급 규칙 — 서버 쪽, 아직 미착수

⚠ 아래는 `/api/cards`가 끝없는 연습 큐를 채울 때 쓸 **서버 로직 의도**다. 프로토타입은
고정된 6장짜리 목업 덱만 써서 이 부분을 실제로 구현·검증한 적이 없다.

- **메인 게이지(완료도=0 잔량 표시)는 폐기했다**(2026-09-04) — 0차가 1,000장을 넘어가면
  숫자가 줄어드는 게 안 보여서 의미가 없어졌다. 그 자리는 위 "오늘 카운터 + 캘린더"가
  대신한다. 승급의 보상은 이제 게이지가 아니라 캘린더 칸이 채워지는 것.
- **뽑기 구성은 여전히 `완료도 0` 70% + 복습 때가 된 상위 차수 30%를 목표로 한다.**
  "때가 됨"은 경과일 기준 — `1차 3일 · 2차 7일 · 3차 21일`. 3단계 채점(이해/애매/모름)이
  없어졌어도 이 가중치 자체는 유효하다 — "0차 카드 우선 + 오래된 카드 가끔"이라는 소거
  모델의 뽑기 비율일 뿐, 채점 결과에 의존하지 않는다.
- ⚠ **정통 간격반복(SM-2/FSRS)은 쓰지 않는다.** "오늘 밀린 47장" 같은 부채 개념이
  생기는데, 그게 바로 Notion에서 복습을 멈추게 만든 압박이고 자유 탐색과도 충돌한다.
  경과일은 **강제가 아니라 뽑기 가중치**로만 쓴다. 채점 UI에서 "몰랐다" 신호를 없앤
  2026-09-04 결정도 같은 방향(부담 요소 제거)이다.

⚠ **iOS Safari에는 진동/햅틱 API가 없다**(iOS 26.5에서 `<input switch>` 우회도 막힘).
보상 피드백은 색·모션·소리로만 설계한다(2026-09-04: 완료 버튼 펄스·세션종료카드가 이
역할). 햅틱이 꼭 필요해지면 그때 Capacitor로 감싼다(Apple Developer $99/년 — 그 전엔
낼 이유 없음).

## 배포

2026-09-03 배포 완료: **https://shorts-deck.shorts-deck.workers.dev**
(Cloudflare 계정 `7maker@gmail.com`, KV `929a2e986f2447689d4bb936327e6120`)

새 PC/새 계정에서 처음 세울 때:

```
npx wrangler kv namespace create KV     # 나온 id를 wrangler.toml에 기입
npx wrangler secret put NOTION_TOKEN    # 크롬 확장이 쓰는 것과 같은 integration 토큰
npx wrangler deploy
```

로컬 개발은 `.dev.vars`에 `NOTION_TOKEN=...` (gitignore됨) 후 `npm run dev`.
카드를 즉시 다시 긁으려면 `/api/sync?restart=1`을 `done:true`가 나올 때까지 호출.

⚠ 그 integration이 대상 DB에 **연결**돼 있어야 한다. 안 되어 있으면 404가 뜬다.

⚠ 신규 `*.workers.dev` 서브도메인은 TLS 인증서 발급에 수 분 걸린다 — 그 사이
`ERR_SSL_VERSION_OR_CIPHER_MISMATCH`가 뜨는 건 정상이고 기다리면 된다.

⚠ 이 PC의 Windows curl(schannel)은 `workers.dev`에 간헐적으로 붙지 못한다 — TLS 핸드셰이크
실패(`HTTP 000`)뿐 아니라 **연결은 되고 응답만 무한 대기하는 형태로도** 나타난다(2026-09-03
실측: 같은 순간 `node -e "fetch(...)"`와 브라우저는 정상 200). Worker 장애나 `503` 재발로
오해하기 딱 좋다 — **검증은 브라우저나 node `fetch`로 할 것. curl 하나로 판단하지 말 것.**

## 파일

```
src/worker.js            API + 정적 서빙. Notion 접근은 전부 여기
wrangler.toml            Worker 설정. KV id는 배포 시 채울 것
package.json             npm run dev / deploy / kv:create
public/index.html        앱 본체 (스와이프·채점·전문 읽기·모아보기). 블록 렌더는 여기
public/manifest.json     홈 화면 웹앱 메타 (standalone·세로 고정)
public/cards.seed.json   폴백 씨앗 21장. ⚠ 사본을 만들지 말 것 — 여기가 유일
prototype.html           Artifact 게시용 스냅샷. 앱과 별개, 동기화 안 됨
tools/build_cards.py     초기 1회 스크래퍼. 입력 파일은 repo에 없어 재실행 불가
```

⚠ `public/cards.seed.json`을 다른 폴더에 복사해 두지 말 것 — 한때 `data/`에 같은
사본이 있었고, 둘을 맞춰 줄 장치가 없어 어느 쪽이 진짜인지 알 수 없는 상태가 됐다.
씨앗을 다시 만들 일이 생기면 `tools/build_cards.py`가 `public/`에 직접 쓴다.
