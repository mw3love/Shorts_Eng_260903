# CLAUDE.md — Shorts Deck

노션에 쌓인 영어 표현을 유튜브 쇼츠처럼 한손 스와이프로 복습하는 웹앱.
이 파일은 git으로 따라다니므로 어느 PC에서 작업하든 동일하게 적용된다.

## 무엇을 푸는 프로젝트인가

크롬 확장 `youtube_dual_subtitle`이 유튜브 자막에서 고른 영어 표현 + AI 해설을
Notion DB `3. 영어 DB`에 자동 저장한다. **저장은 되는데 복습이 안 된다** —
2026-09-03 실측 시점에 **1,323행**이 쌓여 있고 표본 전부 `복습 횟수 = 0`,
`상태 = 비어 있음`이었다. Notion 안에도 `0차 완료`·`1차 완료` 뷰가 이미 있지만
한 번도 안 돌았다. 도구가 없어서가 아니라 **Notion에서 복습하는 게 재미없어서**
멈춘 것으로 본다.

⚠ **행 수를 공개 페이지(`*.notion.site`)에서 세지 말 것.** 표 뷰가 lazy-load라
스크롤 전까지 21행만 렌더한다 — 초기에 이걸 전수로 오독해 "21장짜리 작은 DB"라는
잘못된 전제로 설계할 뻔했다. 정확한 규모는 API의 `databases/{id}/query` 페이지네이션
결과로만 판단한다.

그래서 목표는 "복습 기능"이 아니라 **조작감**이다. 참고 대상: Slidebox(한손 스와이프),
유튜브 쇼츠(무마찰 진행), 클래시 로얄(가변 보상).

## 현재 상태 (2026-09-05 라운드 3 — 최상단) — 여기부터 읽을 것

라운드 2(아래) 배포 후 실기기 피드백을 더 받아 추가로 배포한 것:
- **평소 읽기 중 "안다" 퀵버튼 추가** — 탭=안다(시험 판정과 같은 상태), 롱프레스=
  보관(더 안 보여줌, known도 세팅)/휴지통(로컬 소프트 삭제, 완전삭제 시에만 Notion
  페이지 archived 전환) 갈림길. 새 KV: `archived:v1`, `trashed:v1`. 새 엔드포인트:
  `/api/archive`, `/api/trash`(GET/POST), `/api/trash/restore`, `/api/trash/delete`.
- **이미지 카드 파싱 버그 수정** — `splitBody()`가 image 블록을 "텍스트 없음"으로
  오판해 계속 버리고 있었음(PARSER_VERSION 4에서 이미지 지원을 추가했는데도 여전히
  빈 화면이었던 진짜 원인). v5로 재수집 완료.
- **시트 핸들 `touch-action:none` 추가** — 실기기에서 드래그로 시트가 안 닫히던 문제.
  Playwright 합성 이벤트로는 이 종류의 버그(네이티브 스크롤이 터치를 가로채는 경합)가
  재현이 안 된다는 게 이번에 확인된 구조적 한계 — 다음에 비슷한 "로직은 맞는데 실기기서
  안 됨" 보고를 받으면 `touch-action` 선언 누락부터 의심할 것.

⚠ **디자인 베이크오프 진행 중, 미완료** — 하단 바 아이콘·"안다" 버튼 크기·상단 링칩
크기·시트 챕터 행 레이아웃 4가지를 한 번에 재설계하는 라운드가 진행 중이었는데 세션이
컨텍스트 한계로 끝남. 인계 파일: `.claude/handoff/pending/20260905-1545-design-bakeoff-controls.md`
(다음 세션에서 `/handoff`로 이어감 — 이미 생성한 게이트웨이 이미지 2장의 경로·프롬프트
원문, 참고한 디자인 취향 로그 요약까지 다 들어있음).

## 현재 상태 (2026-09-05 라운드 2 — 실기기 피드백 반영) — 위 라운드3 다음으로 읽을 것

챕터/시험 재설계(바로 아래 "재설계 2026-09-05" 참조)를 배포한 뒤 실제 아이폰으로 써보고
받은 피드백을 반영한 두 번째 라운드. 배포 완료(`Version ID 6cb01555-5190-4e47-9b89-353bcf2c0d78`),
전량 재동기화 진행 중(PARSER_VERSION 4 — image 블록 지원 추가로 전체 재수집 필요).

**바뀐 것**:
- **홈 = 학습(읽기) 화면 자체.** 챕터개요/챕터상세라는 별도 페이지를 없앴다 — 앱을 열면
  바로 카드부터 보임(마지막 위치를 `localStorage`(`sd_pos`)에 저장해 이어봄). "챕터에서
  타고 들어가는 게 번거롭다"는 피드백 반영. 챕터 이동은 하단 시트(전체화면 전환 없이
  살짝 뜸)로만 한다.
- **전체보기/모르는것만/시험보기 3버튼 화면을 없앰.** 읽기 화면 하단 오버레이 바 안에
  필터 세그먼트(전체/모르는 것만)와 시험 퀵버튼을 넣어 한 화면에서 다 처리한다.
- **소챕터·챕터 경계가 자동으로 이어진다.** 소챕터 10장을 스와이프로 다 넘기면 자동으로
  다음 소챕터(토스트로 알림), 챕터(100장)를 다 넘기면 "전체 시험 볼까요?" 넛지 배너가
  뜬다(강제 아님, 넘기면 다음 챕터로 계속). **대챕터 전체(100문제) 시험**을 새로 추가
  (`GET /api/chapterexam?id=cN` — 카드마다 원래 소속 소챕터 id를 붙여 보내, 채점이 각
  소챕터 최고기록에도 정확히 반영되게 함). 하단 시트에서 챕터별로 상시 진입 가능.
- **본문이 화면 전체(가로·세로)를 쓴다.** 컨트롤(스크러버·필터·시험·형광펜·시트버튼)은
  본문 위에 뜬 반투명 오버레이 — 가려지는 부분은 스크롤로 본다는 게 사용자의 명시적
  판단(예전 프로토타입은 버튼 자리만큼 본문 폭을 좁혀뒀었는데, 그게 아깝다는 피드백).
- **스와이프(카드 넘기기) vs 텍스트 드래그(선택) 충돌 해결 시도.** 예전엔 손 대자마자
  포인터를 가로채서(`setPointerCapture`) 텍스트 선택이 잘 안 됐다 — 이제 450ms 안에
  10px 이상 빠르게 움직이면 스와이프로, 그 안에 안 움직이면("길게 누름") 아무것도
  안 하고 iOS 네이티브 선택에 완전히 맡긴다. ⚠ **이 부분은 데스크톱 자동화로는 로직만
  검증했고, 실제 iOS Safari에서의 느낌은 미확인** — 예전 스와이프 커밋 판정도 14라운드
  걸렸듯, 여기도 실기기 왕복이 더 필요할 수 있다.
- **형광펜 버그 수정.** "드래그로 선택부터 하고 형광펜 버튼을 누르면 반응 없음" —
  버튼을 켜는 순간 이미 있는 선택이 있으면 바로 적용하도록 고침(참고한
  `youtube_dual_subtitle` 확장의 원래 동작을 이식할 때 빠뜨렸던 부분).
- **Notion `image` 블록 지원 추가.** 초창기(AI 확장 도입 전) 수동 메모는 스크린샷을
  그대로 붙여넣은 카드가 있어 본문이 빈 화면으로 보였다(파서가 image 블록을 조용히
  버렸음). ⚠ Notion 자체 호스팅 이미지(`file.url`)는 프리사인드 URL이라 약 1시간 뒤
  만료된다 — 캐시(버킷)에 저장하지 않고 표시 시점마다 `GET /api/image?id=`로 새로
  발급받아 302 리다이렉트한다. `external`(고정 URL)만 영구 저장.
- **챕터 개요의 점수를 훨씬 크게.** 하단 시트 챕터 행의 현재/최고 점수 폰트를 22px로
  키움(기존엔 다른 텍스트와 비슷한 크기라 눈에 안 띈다는 피드백).
- 카드 주소 표기를 "1-1"(챕터-소챕터) 형식으로 화면 상단에 항상 작게 표시.

**검증 상태**: 로컬 `wrangler dev` + Playwright로 홈진입/시트/챕터전체시험/소챕터·챕터
경계 자동전환/넛지배너/형광펜 토글까지 재검증(그 과정에서 CSS 버그 2건 추가 발견·수정 —
챕터완주 배너가 `translateY(140%)`로는 실제로 안 숨겨지던 것, 시트 드래그핸들 셀렉터
오류). **프록시검증**일 뿐 — 롱프레스/네이티브 선택 상호작용, iOS 콜아웃 메뉴와 커스텀
팝업의 실제 겹침 정도는 실기기로 확인 전.

## 현재 상태 (2026-09-05 갱신, 최상단) — 여기부터 읽을 것

**2026-09-04까지의 UX(1단 폐지·단일화면·스와이프+완료버튼·0~3차·캘린더·"오늘 스크러버")는
전부 폐기됐다.** deep-interview로 근본 컨셉을 다시 잡음 — 아래 "재설계 2026-09-05"가 지금의
진실이고, 그 아래 "## UX 규약 (2026-09-04 …)" 섹션 전체는 **역사적 기록으로만 남겨둔다**
(왜 지금 구조에 도달했는지 알 수 있게). 새 세션은 헷갈리지 말 것 — 지금 구조를 구현할 때
그 옛 섹션을 참고하지 않는다.

### 재설계 2026-09-05 — 챕터/시험 구조 (구현 완료, 프로덕션 미배포)

**동기**: 로그라이크에서 영감을 얻어 시작했으나(새로고침=런 리셋 등) deep-interview 중
사용자가 스스로 프레임을 두 번 더 크게 바꿨다 — 최종 도착점은 **"공부 범위를 챕터로 쪼개
자유롭게 도전하며 줄여나가는" 구조**(듀오링고식 유닛에 가까움. 순수 로그라이크의 "죽으면
처음부터"는 최종안에 없음). 원래 문제의식(Notion 복습이 방치됨)의 원인은 압박감이 아니라
**Notion 자체의 인터페이스 마찰**(제목→본문→나가서 다음 제목, 무거운 UI, 안다/모른다를
억지로 노션 버튼에 끼워 넣음)이었다는 게 인터뷰 중 사용자가 명시한 핵심 판단 — 그래서 이번
재설계의 최상위 제약은 **"시험/복습 한 사이클의 마찰을 극한까지 줄이는 것"**이고, 게이트
(진도 잠금) 자체는 뺐다(아래 참조).

**결정된 모델**(전부 구현·로컬 검증 완료):
- 전체 카드를 생성일자 오름차순으로 **100장씩 대챕터**, 그 안을 **10장씩 소챕터**로 나눔
  (예: 1,323장 → 대챕터 14개, 마지막 챕터만 23장). 마지막 챕터/소챕터가 100/10장 안
  채워지는 건 의도된 동작 — 별도 처리 없이 그냥 짧은 그룹으로 둔다(사용자가 "일단 앱
  완성하고 나중에 생각하자"고 명시적으로 미룸).
- 카드 상태는 **이진값(안다/모른다)** 뿐 — 노션 0~3차 진도와의 연동은 끊었다. 대신 노션
  속성에 역으로 반영하는 건 이번 범위 밖(보류, 필요해지면 나중에).
- **게이트(진도 잠금) 없음** — 모든 챕터·소챕터가 처음부터 자유 열람. "최고기록"은 순수
  진행 현황판이지 다음 챕터 진입 조건이 아니다(사용자가 명시적으로 이 방향을 선택 —
  "범위를 자유롭게 넘나들며 줄여나간다"는 목표와 하드 게이트가 상충한다고 판단).
- **학습(복습)**: 소챕터 10장(또는 "모르는 것만")을 기존 스와이프+블록 렌더링 UI로 훑어봄
  (채점 없음, 완료/되돌리기 버튼 자체가 없음). **되돌리기 버튼은 완전히 폐기**(자기평가는
  "정직하게 판단하고 넘어간다"가 핵심 가치라 무를 수 있게 하면 그 가치가 흐려진다는 판단
  — 잘못 눌렀으면 재시험에서 정정).
- **시험**: 제목만 표시 → "정답 확인" 탭 → 본문 리빌 → "알고 있었음/몰랐음" 2단계
  자기평가로 최종 확정. 문제 순서는 매번 섞는다. 소챕터 통과 기준(5/10)·대챕터 통과
  기준(80/100)은 **게이트가 아니라 뱃지/현황판 숫자**일 뿐.
- **최고기록은 단조증가**(재도전해서 점수가 낮아져도 안 내려감) — 현재 라이브 점수와
  별개로 KV에 따로 저장.
- **삭제 버튼은 이번 범위에서 뺌**(되돌리기가 없어지며 "삭제→복구" 쌍이라는 도입 이유
  자체가 사라짐).
- **하단 스크러버는 폐기 안 함** — "오늘 본 카드" 트래커였던 예전 역할 대신, **소챕터
  10장 안에서 빠르게 훑어보는 네비게이션**으로 재활용(드래그하면 그 카드로 직접 이동).
- **캘린더 완전 폐기** — 날짜 개념 자체를 버림. 그 대신 챕터 개요 화면(대챕터별
  현재/최고 점수 한눈에)이 홈 화면이 됨. 별도 진입 버튼이 필요 없음(홈 자체가 개요).
- **부가 기능 2개, 같은 라운드에서 추가**:
  - **제목 고치기**(`POST /api/retitle`) — Notion DB가 AI 질의응답 기준이라 제목(이름)에
    단어 하나·한글 질문이 그대로 들어간 행이 섞여 있음. 본문에서 예문을 드래그 선택하면
    "이 문장을 제목으로" 팝업이 뜨고, 탭하면 그 카드의 **실제 Notion 제목 자체를 고침**
    (미러 아님). `cardKey`가 바뀌므로 이미 "안다"였던 카드는 새 키로 이어받는다.
  - **형광펜**(`POST /api/highlight`) — `youtube_dual_subtitle` 확장(같은 저장소
    `C:\Users\7make\Dev\youtube_dual_subtitle`)과 동일한 상호작용을 이식: 헤더의 🖍
    버튼을 켜면 모드가 유지되고, 그 뒤 본문에서 드래그할 때마다 자동으로 그 구간의
    Notion 인라인 코드(code) annotation을 토글한다(이미 형광펜된 부분을 다시 클릭해도
    해제됨). 이것도 **KV(버킷)와 Notion 블록 양쪽에 실제로 반영**(미러 아님).
    이미 code=true였던 부분과 아닌 부분이 섞인 선택은 "새로 칠하기"로 본다(칠해진 부분은
    그대로, 안 칠해진 부분만 칠함) — 확장의 정책을 그대로 가져옴.

**⚠ 블록 포맷에 `id`(원본 Notion block id)를 추가했다(`PARSER_VERSION` 2→3)** — 형광펜을
Notion에 되쓰려면 그 블록의 진짜 id가 필요해서다. 이 때문에 **재배포 후 반드시
`/api/sync?restart=1`로 전체 재동기화가 필요**(모든 카드 본문을 다시 긁어야 `id`가 채워짐
— 기존 v2 캐시엔 없음).

**개발 중 발견·수정한 실제 버그 5개**(실기기 아니라 로컬 `wrangler dev`+Playwright로
직접 클릭해보며 찾음 — 코드만 보고는 못 잡았을 것들):
1. `chunk(arr,size)` 헬퍼가 `return out`을 빼먹어서 `/api/chapters` 등이 전부 500 에러.
2. `.navbtn{display:flex}`가 브라우저 기본 `[hidden]{display:none}`보다 author 스타일이라
   우선순위가 높아서, 뒤로가기/형광펜 버튼에 `hidden` 속성을 줘도 계속 보였음 — author
   CSS가 UA 기본 스타일을 항상 이긴다는 것 자체를 놓치기 쉬운 함정. `[hidden]` 규칙을
   쓰는 커스텀 `display` 요소마다 `.class[hidden]{display:none}`을 따로 명시해야 한다
   (같은 패턴이 `.examjudge`에도 있어서 같이 고침).
3. 같은 부류의 두 번째 함정 — `#viewStudy{display:flex}`(ID 선택자)가 `.view[hidden]`
   (클래스+속성)보다 명세 우선순위가 높아, 학습 화면을 hidden 처리해도 여전히 클릭을
   가로챘다(Playwright가 "다른 요소가 클릭을 가로챈다"고 정확히 알려줌). ID 규칙을 새로
   추가할 때마다 그 요소의 `[hidden]` 케이스를 별도로 다시 이겨야 한다는 걸 잊기 쉽다.
4. 시험(exam) 화면에서 정답 리빌 후 제목이 위로 잘려 보임 — `exambody`가 `class="content
   exambody"`로 학습 화면의 `.content{min-height:100%}`를 그대로 물려받아, `justify-
   content:center` 플렉스 안에서 부모 높이만큼 억지로 늘어나며 위로 넘쳤다. 클래스를
   재사용할 때 그 클래스에 딸려오는 스타일까지 같이 딸려온다는 걸 놓친 것 — 재사용
   대상 클래스의 전체 규칙을 다시 훑어야 한다.
5. `judgeCard`가 `/api/answer` 응답을 기다리지 않고(fire-and-forget) 바로 다음 카드로
   넘어가서, 마지막 문제의 최고기록 갱신이 결과 화면에 반영되기 전에 결과 화면이 그려지는
   경쟁 상태가 있었다 — 카드당 한 번뿐인 탭이라 응답을 기다려도 무마찰 원칙에 안 걸려서
   `await`로 고침.

**배포 완료(2026-09-05)** — 사용자 확인 후 `npx wrangler deploy` 실행, `Version ID
e9d4ff0c-b8c1-4a47-957b-04b46c413c59`. 배포 직후 `/api/sync?restart=1`로 전량
재동기화도 시작함(PARSER_VERSION 3 — block id 채우기 목적). 기존 KV의 0~3차/복습횟수
값은 새 코드가 안 읽으므로 그대로 방치(정리 안 함, 무해).

**남은 것**: 실기기 검증(사용자가 직접). 챕터개요·챕터상세·학습·시험·제목고치기·형광펜
전부 로컬에서 실제 클릭으로 검증됐지만, 스와이프 손맛·형광펜의 실제 Notion 반영은
실기기/실계정에서 직접 확인 필요.

## 현재 상태 (2026-09-04 갱신, 역사 기록 — 위 2026-09-05 재설계로 대체됨)

**배포된 것**(2026-09-04): 새 UX(1단 폐지·단일화면·스와이프=이동/버튼=채점·하단 시트
캘린더·오늘 스크러버) 전체가 `public/index.html`에 이식되고 `wrangler deploy`까지 끝났다.
실제 서비스 URL(`https://shorts-deck.shorts-deck.workers.dev`)에서 실제 Notion
데이터(1,323장)로 카드 렌더·완료(승급)·롱프레스 텔레포트·되돌리기·검색/차수탭·캘린더
날짜별 목록·세션 진행·오늘 스크러버까지 전부 실조건검증 마침(1라운드는 브라우저 자동화,
2라운드는 사용자 실기기 피드백 → 수정 → 재검증). 상세는 `Confidence` 트레일러가 붙은
해당 커밋들 참고.

⚠ **채점 모델이 이번에 백엔드까지 같이 바뀌었다** — 예전 "이해/애매/모름" 3단 채점(`grade`
필드)을 완전히 버리고, 클라이언트가 목표 차수(`step`, 0~3)를 직접 계산해 보내는 방식으로
단순화했다(탭=`min(현재+1,3)`, 롱프레스=임의 차수). 상세는 아래 "아키텍처" 섹션의
`POST /api/grade` 설명 참고. **상태(난이도) Notion 컬럼은 더 이상 채점이 건드리지 않는다**
(3단 신호 자체가 없어졌으므로) — 사용자가 그 컬럼을 다른 용도로 쓰고 싶으면 자유.

⚠ **"오늘 본 개수"의 트리거·되돌리기 동작이 한 번 더 바뀌었다(2026-09-04, 실기기
피드백으로 뒤집음)** — 첫 배포 땐 KV 쓰기 예산을 아끼려고 "채점 이벤트"에만 올라탔는데,
실기기에서 써보니 스와이프만 하면 카운터가 안 움직여 부자연스러웠다. 트리거를 "카드가
화면에 표시될 때"(`POST /api/seen`)로 되돌렸다 — 걱정했던 쓰기 비용은 실제로는 문제가
안 된다: 같은 카드를 다시 봐도 키 중복 제거로 쓰기가 또 안 나가서, 실제 쓰기 횟수는
"그날 처음 본 새 카드 수"만큼만 발생한다(하루 200장을 봐도 200회, 무료 1,000회/일에
전혀 안 걸림). **되돌리기도 이제 "봤다"는 사실 자체를 되돌린다**(`POST /api/unseen`) —
단, 매번이 아니라 **그 이동이 그 카드의 오늘 첫 조회였을 때만**(클라이언트 히스토리
스택의 `firstSeenToday` 플래그로 판단 — 그날 이미 다른 경로로 본 카드면 안 뺀다).
예전 "되돌리기는 본 사실은 안 건드린다"는 결정은 폐기.

✓ **하단 "오늘 스크러버" 신설** — 오늘 본 카드 수만큼 점을 균일 간격으로(첫 점은 항상
맨 왼쪽) 그리는 얇은 수평 바. 카드를 넘길 때마다 점이 하나씩 늘고, 드래그하면 가장
가까운 점으로 스냅해 그 카드를 "엿본다"(라이브 `deck`/`idx`/히스토리는 안 건드림 —
순수 미리보기). 엿보는 중에 완료 버튼을 누르면 그 카드가 라이브 위치로 승격된 뒤
정상 채점을 탄다(`promotePeekToLive()`). 되돌리기는 엿보기 상태를 무시하고 항상 진짜
마지막 라이브 액션을 대상으로 하며, 결과(새 라이브 위치)로 화면이 넘어간다 — 스크러버로
딴 데를 보고 있어도 되돌리기는 예상대로 동작한다. 점 개수 상한은 없음(초기 결정,
너무 촘촘해지면 그때 재검토).

⚠ **API 라우팅 함정: 핸들러가 이미 `Response`를 반환하면 라우터에서 `json()`으로
다시 감싸지 말 것.** `/api/seen`·`/api/unseen`을 처음 추가할 때 `return
json(await handleSeen(...))`로 이중 래핑해서, 응답이 항상 빈 객체(`{}`)로 나가는
버그가 있었다(`JSON.stringify(Response객체)`가 `"{}"`가 됨 — 2026-09-04 실제 배포
후 발견·수정). `handleCards`/`handleIndex`/`handleCalendar`/`handleDay`처럼 핸들러가
이미 `json(...)`을 반환하는 타입이면 라우터는 `return handleX(...)` 그대로 — 반면
`writeGrade`/`undoGrade`/`syncStep`처럼 **일반 객체**를 반환하는 타입만 라우터에서
`json(await ...)`로 감싼다. 새 엔드포인트를 추가할 때 이 두 패턴을 헷갈리지 말 것.

✓ **실기기 검증 완료(2026-09-04, 2라운드)** — 1라운드에서 사용자가 실기기로 스와이프해도
오늘 카운터가 안 움직이는 문제를 발견 → 위 두 항목(트리거·되돌리기)으로 수정하고 스크러버
신설 → 데스크톱 브라우저 자동화로 재검증 후 배포. **스크러버 자체는 아직 실기기 미확인**
(드래그 손맛은 대리 불가 — 다음에 사용자가 확인할 것).

✓ **3라운드(같은 날) — 스크러버 레이아웃 겹침 수정 + 메인 덱 순서를 생성일순으로 교체.**
2라운드 배포 직후 사용자가 실기기에서 스크러버 선이 본문 텍스트 위에 겹쳐 보인다고 지적
→ `.scrubber`가 `.stage` 안에 `position:absolute` 오버레이로 있던 게 원인, `#app`의
독립된 flex 자식 행으로 옮겨 자기 공간을 확보하도록 고침(더 이상 스크롤되는 본문과
같은 층을 안 씀). 같은 라운드에서 카드 뽑는 순서에 대한 질문이 나와, 완료도 0/복습
가중치 뽑기를 전부 버리고 **생성일자 오름차순**(Notion에서 쓰던 방식)으로 교체 —
상세는 아래 "메인 덱 공급 규칙" 참조.

⚠ **`npx wrangler deploy`는 반드시 프로젝트 폴더 안에서 실행할 것.** 홈 디렉토리
(`C:\Users\7make`)에서 실행하면 `wrangler.toml`을 못 찾아 "autoconfig"가 현재 폴더
전체를 훑다가 Windows의 레거시 호환 정션(`Application Data`→`AppData\Roaming`)
재귀 탐색에서 `EPERM: scandir` 오류로 죽는다(2026-09-03 실측, 정션 자체는 정상 —
엉뚱한 위치에서 실행한 부작용). 프로젝트 폴더로 `cd` 후 실행하면 바로 통과한다.

⚠ **`wrangler dev` 전에 `npm install`부터 할 것.** `node_modules`가 없으면 `npx wrangler
dev`가 그 자리에서 wrangler를 새로 받으려다 조용히 멈춘 것처럼 보인다(2026-09-04 실측 —
로그가 `npm warn exec ... will be installed`에서 몇 분째 안 움직임). `npm install` 먼저
끝내고 `npx wrangler dev`를 돌리면 즉시 뜬다.

⚠ **로컬 `wrangler dev`(플래그 없이)는 로컬 전용 미니 KV를 쓴다 — 실제 배포 데이터와
다르다.** 진짜 1,323장으로 켜보려면 `wrangler dev --remote`로 실제 KV·Notion에 붙어야
한다(단, 이 모드에서의 테스트용 채점·되돌리기 호출은 **진짜로 Notion 페이지를 수정한다** —
테스트 후 반드시 되돌리기(undo, `prev:null`)로 원상복구할 것. 2026-09-04 실측: 캘린더
KV 로그는 되돌리기로 안 지워지므로 테스트로 채운 `daily:v1:*` 키는 `wrangler kv key
delete`로 직접 지워야 한다).

⚠ **`wrangler kv key delete`(`--remote`) 직후 배포된 워커의 API 응답엔 최대 수십 초간
옛 값이 그대로 보일 수 있다** — KV 읽기 엣지 캐시의 정상적인 지연이다(2026-09-04 여러
차례 실측). `wrangler kv key get ... --remote`로 스토리지 자체를 직접 조회하면 즉시
반영돼 있다(404면 진짜로 지워진 것) — API가 옛 값을 보여준다고 삭제가 실패했다고
오해하지 말 것. 급하면 20~30초 기다렸다 API를 다시 확인.

남은 것: 스크러버 실기기 확인(드래그 손맛 + 겹침 수정이 실제로 해결됐는지). 그 외엔
없음 — 다음은 실사용 며칠 지켜보며 나오는 피드백에 달림.

### 해결된 것 — 다시 밟지 말 것

**옛 `503` 스톨(1,101/1,323에서 6/6 실패)의 원인은 상태 값 비대가 맞았다.**
`sync:state`에 누적 카드를 통째로 담아 약 1.7MB까지 커졌고, 매 단계 그걸 파싱·직렬화하다
무료 요금제 CPU 10ms를 넘겼다. 인덱스+버킷으로 쪼개 값 크기를 유계로 만든 뒤 **같은
지점을 그냥 통과해 1,323까지 갔다** — 가설이 반증되지 않고 확인된 것.

**카드 본문은 이제 Notion 원본 블록으로 보관한다**(평문 문자열 배열 폐기). 렌더는 표시
시점에 클라이언트가 한다 — 그래서 **앞으로 렌더링을 몇 번 바꾸든 재동기화가 필요 없다.**

⚠ 카드별 개별 KV 저장은 여전히 불가 — 무료 **KV 쓰기 1,000회/일** 제한에 1,323장이 걸린다.
⚠ 파서를 바꾸면 카드의 `parserVersion`을 올릴 것. 증분 로직이 그 값이 다르면 재사용하지
않고 다시 긁는다(현재 `2`). 이게 없으면 옛 포맷이 영원히 살아남는다.

**`undoGrade`의 스코프 버그(2026-09-04 실조건 테스트로 발견·수정)** — `n`·`step`을
`try` 블록 안에서 `const` 선언해 놓고 블록 밖 `return`에서 참조해, 되돌리기를 호출할
때마다 `"n is not defined"` 500 에러가 났다. 채점 재설계 이전부터 있던 결함(3단 채점
시절 코드도 같은 구조)인데 그동안 실사용에서 되돌리기를 실제로 호출해 본 적이 없어
안 걸렸던 것으로 보인다 — `n`/`step` 선언을 `try` 밖으로 옮겨 해결.

## 상류 데이터 — Notion DB 실측 (2026-09-03)

⚠ **2026-09-05부터 앱은 `완료도`·`상태`(복습 횟수 포함) 컬럼을 전혀 안 읽고 안 쓴다** —
0~3차 진도와의 연동을 끊었기 때문(위 "현재 상태" 참조). `discoverSchema()`도 이제 title/url만
찾는다. 아래 표는 Notion DB 자체의 컬럼 구조 기록으로만 남겨둔다(사용자가 이 컬럼들을
다른 용도로 계속 쓰고 싶으면 자유 — 앱이 안 건드릴 뿐).

DB: `3. 영어 DB` (`277763872455804ebc00cb991e8e3247`, 워크스페이스 `mw2love`)

| 속성 | 타입 | 쓰임 |
|---|---|---|
| 이름 | title | 카드 앞면(영어 문장). 자막 문장 우선 |
| URL | url | 원본 유튜브 링크 — 카드에서 바로 점프 |
| 복습 횟수 | number | **본 총 횟수**(단조 증가). 차수와 별개 축 |
| 상태 | select | **체감 난이도** — 쉬움 / 적당함 / 어려움 / 완료 |
| 완료도 | select | **진도(차수)** — `0` / `1차 완료` / `2차 완료` / `3차 완료` |
| 날짜 / 생성 일시 | date / created_time | 저장 시각 |
| 복습 완료 · 적당함 | button | ⚠ API로 쓸 수 없음 — 무시한다 |

⚠ **`상태`와 `완료도`는 이름이 비슷하지만 다른 축이다.** 채점의 체감 난이도는
`상태`로, 복습을 몇 차 돌았는지는 `완료도`로 간다. 그리고 **둘 다 `select` 타입이라
타입으로는 구분이 불가능하다**(2026-09-03 사용자가 `완료도`를 status→select로 변경).
그래서 `discoverSchema()`는 타입이 아니라 **옵션 내용**으로 역할을 판별한다 —
`쉬움|어려`가 있으면 난이도, `N차 완료`가 있으면 진도. 첫 구현은 타입으로 골랐다가
진도 컬럼을 집어 채점이 조용히 유실됐다.

⚠ **판별 결과(`schema:v1`)를 KV에 캐시하는데, 이게 낡으면 채점이 또 조용히 유실된다.**
`writeGrade()`는 이 캐시의 `progressType`을 그대로 믿고 `{select:...}`냐 `{status:...}`냐를
정한다. 2026-09-03 실측: 사용자가 `완료도`를 status→select로 바꾼 뒤에도 KV엔 옛 status
스키마가 남아 있어 PATCH가 검증에 걸렸고, `writeGrade`가 예외를 삼키는 구조라 앱은 아무
일 없다는 듯 넘어갔다(응답의 `notionOk:false`가 유일한 신호였고 프런트는 그걸 안 본다).
→ `syncStep()`이 새 동기화를 시작할 때마다 `schema:v1`을 다시 쓴다. **그 한 줄을 지우지
말 것.** DB 스키마를 손으로 바꿨으면 `/api/sync?restart=1`로 캐시를 갱신하는 게 확실하다.

⚠ `완료도`의 첫 옵션은 `0차`가 아니라 그냥 **`0`** 이다. 화면에 "0차"로 보이려면
앱에서 라벨링한다. 빈 값도 `0`과 같게 취급한다.

⚠ `적당함`은 값이 아니라 **버튼 속성 이름**이다. 표 뷰에서 모든 행에 "적당함"이 찍혀
보이는 건 버튼 라벨이지 데이터가 아니다(초기에 이걸 데이터로 오해했다).

⚠ **속성 이름을 하드코딩하지 말 것.** 위 판별은 전부 타입 + 옵션 내용 기반이다.
크롬 확장도 같은 전략을 쓴다(사용자가 컬럼 이름을 바꿔도 안 깨짐).

### 카드 뒷면은 DB 컬럼이 아니라 "페이지 본문"에 있다

```
페이지 본문
├── 자막 문맥 (21장 중 12장)
├── 「영어표현 + 한국어 뜻」 한 줄     ← 1단계 힌트. 21/21 모든 카드에 존재
├── 해설 문단 (구성·의미·관통하는 이미지)
└── 표 (어원/예시)                    ← 2단계 전체 해설
```

이 구조가 힌트(뜻)/해설을 분리해 준다 — 힌트는 강조 표시로, 해설은 그 아래 이어서, 2026-09-04
UX 재설계 이후로는 **탭 없이 한 화면에 같이** 보여준다(1단 폐지). 파싱은
`src/worker.js`의 `splitBody()`. 그래서 카드 하나를 만들려면 **행 조회 + 본문 조회 2회**가
필요하고, Notion은 평균 3 req/s라 실시간 조회가 불가능 → KV 캐시가 선택이 아니라 필수.

## 아키텍처 — 왜 이 모양인가

**Cloudflare Worker 하나가 앱과 API를 같은 출처에서 서빙한다.**

⚠ **`api.notion.com`은 `Access-Control-Allow-Origin`을 보내지 않는다**(2026-09-03 curl 실측).
브라우저 페이지는 Notion API를 직접 못 부른다. 크롬 확장이 되는 건 `host_permissions`로
CORS를 면제받기 때문이지 Notion이 허용해서가 아니다. 게다가 토큰을 프런트에 두면 털린다.
→ 순수 정적 호스팅(GitHub Pages 등)만으로는 이 앱이 성립하지 않는다.

**기각한 대안:** GitHub Pages + Actions. 무료 개인 계정은 private repo로 Pages 호스팅이
아예 불가하고, 유료로 올려도 게시된 사이트는 기본 public이다. 게다가 grade 쓰기 경로가
없어 결국 Worker를 또 붙여야 한다 — 컴포넌트 2개보다 1개가 낫다.

### ⚠ 무료 요금제는 요청당 서브요청 50회 — 그래서 동기화가 청크로 쪼개져 있다

카드 하나를 만들려면 본문 블록 조회가 1~2회(표가 있으면 2회) 필요하다. 21장만 해도
`스키마 1 + 행 조회 1 + 본문 21 + 표 21 ≈ 60회`로 상한을 넘는다. 첫 구현이 이걸 한
요청에서 다 하려다 응답 없이 멈췄다.

**"표 조회를 빼서 아끼자"는 해법이 아니다** — 이 프로젝트의 전제가 "카드가 계속 쌓인다"라
절약형 최적화는 시간만 벌고 언젠가 반드시 다시 터진다. 그래서:

- `syncStep()`이 **서브요청 예산(`SUB_BUDGET = 40`)만큼만** 처리하고 진행 커서를
  KV `sync:state`에 남긴다. 버킷이 50장 차면 즉시 flush하므로 상태 값은 항상 유계다.
- cron(`*/5 * * * *`)이 알아서 이어 돌린다. 진행 중이면 계속, 끝났고 인덱스가 6시간보다
  낡았으면 새로 시작.
- `GET /api/cards`는 **Notion을 부르지 않는다** — 인덱스에서 뽑고 해당 버킷만 읽는다.
  아직 인덱스가 없으면 `202 + syncing:true`를 주고, 앱은 씨앗으로 퇴화한다.
- `GET /api/sync?restart=1`로 수동 강제 재동기화.

⚠ **KV의 `get`/`put`/`list`/`delete`도 이 50회에 포함된다**(2026-09-03 공식 문서 확인).
`fetch`만 세면 안 된다 — 버킷을 여러 개 읽는 경로에서 특히 위험하다. 그래서 동기화
루프의 KV 접근은 `kvGet`/`kvPut`으로 감싸 같은 예산에서 차감한다.

카드 수가 늘어도 cron 틱이 늘 뿐 설계는 그대로다.

### ⚠ 전량을 폰에 내려보내지 않는다

1,323장 본문은 약 2MB다. 매번 받으면 셀룰러 첫 로딩이 느리고 Safari `localStorage`
한도(~5MB)에도 부딪힌다. 그래서 **본문은 필요한 만큼만** 서버에서 골라 준다
(`pickCandidates()`, 뽑기 규칙은 아래 UX 규약 참조). 앱은 8장씩 받아 큐가 3장 아래로
줄면 조용히 더 채우므로, 사용자 입장에선 끝이 없다.

⚠ 단 **인덱스(제목·차수만)는 통째로 내려도 된다** — 약 100KB다. 모아보기 목록 뷰가
`GET /api/index`로 이걸 그대로 쓰고, 카드 하나를 펼칠 때만 `GET /api/card?id=`로 본문을
따로 받는다.

**Artifact를 프로덕션으로 쓸 수 없는 이유:** Artifact의 CSP가 외부 fetch를 차단한다.
`prototype.html`(Artifact로 게시됨)은 데이터를 인라인한 **조작감 테스트용 스냅샷**일 뿐이고,
실제 앱은 `public/index.html`이다. 둘은 별개 파일이며 자동 동기화되지 않는다.

## ⚠ pageId는 안정적인 키가 아니다

크롬 확장은 재저장(형광펜 추가 후 다시 저장)을 **"새 페이지 생성 + 옛 페이지 archive"**로
구현한다(Notion API에 본문 통째 교체가 없어서 — 함정 상세: `~/.claude/wiki/notion-페이지-덮어쓰기-재저장.md`).
그래서 **재저장할 때마다 pageId와 URL이 바뀐다.** 확장 코드 주석은 이걸
"단어장 페이지엔 사실상 무해"라고 적어 뒀는데, **복습 앱이 생기는 순간 그 가정이 깨진다** —
pageId를 키로 학습 이력을 저장하면 재저장 한 번에 그 카드의 이력이 통째로 사라진다.

→ 학습 이력의 키는 **정규화된 제목**(`cardKey()`)이다. pageId는 Notion에 쓸 때만 쓴다.

⚠ 2026-09-05 `/api/retitle` 추가로 **의도적으로 cardKey가 바뀌는 경우**가 새로 생겼다(사용자가
본문에서 예문을 드래그해 제목을 고치는 기능) — 이건 위의 "재저장 churn"과는 다른 층위다.
`handleRetitle()`이 옛 키의 `known:v1` 항목을 새 키로 직접 옮겨주므로 안다/모른다 판정은
안 끊긴다. 반면 pageId·버킷 소속은 안 바뀐다(같은 페이지, 제목만 수정).

## 저장 위치 — 무엇을 어디에 두는가

| 데이터 | 위치 | 이유 |
|---|---|---|
| 카드별 안다/모른다 | Worker KV `known:v1` | `{ [cardKey]: true }`(안다만 기록, 모른다=부재). 진실의 원본 — 노션 0~3차와 연동 끊음(2026-09-05) |
| 소챕터/대챕터 최고기록 | Worker KV `best:v1` | `{ sub:{id:n}, chap:{id:n} }`. 단조증가(재도전해서 낮아져도 안 내려감) — 게이트 아님, 현황판용 |
| 보관(더 안 보여줌) | Worker KV `archived:v1` | `{ [cardKey]: true }`. known/총량 점수엔 영향 없음 — 전체보기·시험 로테이션에서만 제외 |
| 휴지통(소프트 삭제) | Worker KV `trashed:v1` | `{ [cardKey]: true }`. "진짜 삭제" 전까진 Notion 원본 그대로 — 로컬에서만 숨김 |
| 카드 목록 | Worker KV `index:v2` | 제목·버킷번호만. 약 100KB — 통째로 내려도 됨. 챕터/소챕터는 이 배열을 매 요청마다 생성일자순으로 슬라이싱해 즉석 계산(별도 저장 안 함) |
| 카드 본문 | Worker KV `bucket:v2:N` | 50장 단위. 블록마다 원본 Notion `block.id` 포함(v3, 형광펜 되쓰기용) |
| 동기화 커서 | Worker KV `sync:state` | 진행 중일 때만 존재. 끝나면 삭제 |
| 카드 제목·형광펜 | **Notion 본문/속성 자체** | 미러 아님 — 실제 원본 수정(`/api/retitle`, `/api/highlight`). 실패해도 로컬 KV는 먼저 반영(마찰 최소화), `notionOk`로 정직하게 알림 |
| 오프라인 캐시 | localStorage | ⚠ 보조일 뿐 — iOS는 영구성을 보장하지 않는다. 글자 크기 설정만 여기 저장 |

⚠ (역사) 예전 `grades`(0~3차+횟수)·`daily:v1:*`(캘린더)·`schema:v1`(진도/난이도 판별 캐시)
키는 2026-09-05 재설계로 전부 안 쓴다. 프로덕션에 남아 있어도 새 코드가 안 읽으니 무해 —
정리하고 싶으면 `wrangler kv key list`로 찾아 지워도 되고 안 지워도 상관없다.

⚠ **iOS PWA의 localStorage/IndexedDB를 진실의 원본으로 삼지 말 것.** 홈 화면 앱은
7일 만료를 면제받지만 Safari 방문기록 삭제·저장공간 부족 시 함께 지워진다.

## UX 규약 (2026-09-04, 역사 기록 — 2026-09-05 챕터/시험 재설계로 전면 대체됨, 새로 구현할 때 참고하지 말 것)

⚠ 이 섹션 전체(캘린더·스와이프+완료버튼·0~3차·오늘 스크러버·되돌리기)는 위 "현재 상태"의
2026-09-05 재설계로 대체됐다. "왜 예전엔 이렇게 했었나"를 알고 싶을 때만 읽는다.

### (역사) UX 규약 (2026-09-04 전면 재설계 — `prototype.html` 목업에서 12라운드 확정, `public/index.html`에 이식·배포 완료)

이 섹션은 **목업(`prototype.html`)에서 확정한 설계**를 기술하며, `public/index.html`
(실제 배포 앱)에도 그대로 이식돼 있다(2026-09-04). 세션 데이터 모델(캘린더 서버화, 채점
모델 변경 등)은 이식 과정에서 실제 백엔드에 맞게 구체화됐다 — 그 부분은 위 `현재 상태`
섹션과 아래 `아키텍처` 섹션에 별도로 적어 뒀다.

### 핵심 변경 — 1단(카드) 완전 폐지

탭 1회면 바로 본문이 보이던 흐름(2026-09-03 "2단 공개 폐기" 결정)을 밀어붙여 아예
**단일 화면**으로 만들었다: 화면 하나 = 제목(항상 최상단, 크게) + 본문(Notion 블록 그대로).
힌트 줄을 따로 뽑아 굵게 강조하던 것도 없앴다 — 그 줄이 항상 "뜻"이라는 보장이 없어서
(사용자 지적. 21/21 표본은 확인됐지만 앞으로도 그러리란 보장은 없음), Notion 원본 서식
(굵게 등)을 그대로 믿는 쪽으로 갔다.

### 제스처

| 제스처 | 동작 |
|---|---|
| 좌우 스와이프 | 이전/다음 카드로 이동(채점 없음). 제목+본문이 카드처럼 같이 슬라이드, 옆 카드의 제목+본문이 실제 밝기 그대로(하단만 페이드) 같이 딸려 들어옴 — 예전엔 옅게(불투명도 .5) 죽였는데 스왑 순간 밝기까지 튀어 보여서 뺐다(2026-09-04 15라운드) |
| 상하 | 네이티브 스크롤 |
| 완료 버튼 탭 | n+1(다음 차수로 승급) |
| 완료 버튼 롱프레스(0.45초, 진행 링 표시) | 0/1/2/3차 미니 팝업으로 직접 텔레포트 |
| 되돌리기 버튼 | 이동+채점 통합 히스토리 스택에서 한 단계씩 되짚음(다단계, 연타로 오늘 한 것 전부 취소 가능) |

⚠ **채점에 "몰랐다/틀렸다" 신호가 없다.** 스와이프로 그냥 넘기면 스킵일 뿐 — 강등
메커니즘 자체를 없앴다(2026-09-04 결정, 옛 3단계 이해/애매/모름 채점·"복습 횟수"에 따른
강등·"다시 만남" 보너스 전부 폐기).

⚠ **되돌리기는 "봤다"는 사실(오늘 카운트)까지는 안 되돌린다** — 채점(차수)만 되돌리고
그 카드를 오늘 열어봤다는 기록은 남는다. 예전(2026-09-03)의 "직전 1건만, 복습 횟수까지
원상복구" 규약은 폐기.

**스와이프 커밋 판정은 고정 px가 아니라 폭 비율+속도 하이브리드다**(2026-09-04, Swiper.js
`longSwipesRatio`/`longSwipesMs`·Material Design 3 duration 토큰 참고해 재설계) — 천천히
끌 땐 화면폭의 50% 이상, 빠르게 튕기면(300ms 안에 18% 이상) 짧은 거리로도 커밋. 옛 고정
px(`TH`) 방식은 화면이 크면 살짝만 끌어도 넘어가버리는 문제가 있어 폐기. 코드는
확인했지만("자체확인") 실기기 체감 검증 전.

⚠ 좌우 스와이프와 Safari 뒤로가기 제스처는 거의 간섭하지 않는다 — iOS 뒤로가기는
**화면 좌측 끝에서 시작**해야 걸리고, 우리 제스처는 중앙에서 시작한다(2026-09-03 확인,
단일화면이 돼도 여전히 유효).

### 화면 구성

- **최상단 줄**(카드와 무관, 드래그해도 안 움직임): 오늘 카운터(중앙 — 오늘 "본" 고유
  카드 수, 채점 여부 무관. 세션 진행 중엔 `n/total`로 바뀜) + 생성일(우측, 차수색 배지).
  우상단은 시선이 잘 안 가서 상단 정보를 여기로 몰았다(2026-09-04 사용자 판단).
- 그 아래 제목(크게, 좌우 드래그 시 본문과 같이 이동) — 항상 최상단 근처 고정은 유지.
- 본문 인라인 코드(사용자가 브라우저 확장으로 친 형광펜) = **코랄 실색 배경 + 어두운
  텍스트**("하이라이터 반전" — 코랄 텍스트를 코랄 배경 위에 얹던 이전 안은 대비가 낮아
  폐기, 실측 대비비로 확인).
- **강조색은 코랄 하나로 통일**(표 헤더, 시트 활성 탭, 슬라이더 손잡이 등). 예전에 쓰던
  gold는 그 의미(진행 게이지·다시 만남 보너스 테두리)가 이번 재설계에서 다 빠져서 폐기.
  차수(0/1/2/3차) 표시만 예외로 회색/파랑/주황/빨강 4색 유지 — 카테고리 4개를 단색으로
  못 나타내서("다른 색이 꼭 필요한" 경우로 판단).
  ⚠ **캘린더 개수 숫자는 예외**(2026-09-04 `design-bakeoff` 라운드3 확정, `prototype.html`
  반영 완료) — 코랄은 "오늘" 칸 테두리(링+글로우)에만 쓰고, 개수 숫자 자체는 밝은 흰색
  (`--paper`, `font-weight:300`)으로 뚜렷하게: 강조색을 "값 표시"가 아니라 "의미 있는
  상태"에만 쓴다는 원칙(`~/.claude/design-system/preferences.md`)과 맞춘 것 — 흰색도
  코랄(강조색)은 아니라서 원칙 자체는 안 깨짐. 칸 모양은 그림자·그러데이션 없는 플랫
  (`border-radius:5px`, `background:var(--ink3)`). 상세: `~/.claude/design-system/
  projects/shorts-deck.md`.
- 완료 성공 시 버튼이 짧게 펄스. 세션(날짜별/목록 복습)이 끝나면 짧은 완료 카드(체크
  아이콘 + "오늘 N개 복습 완료", 0.9초) 후 평소 화면 복귀.
- 2단이 없어졌으므로 글자 크기 조절은 **제목/본문 따로**(하단 시트 최상단 슬라이더),
  값은 `localStorage`에 저장돼 다음 접속에도 유지.

⚠ 채점이 완전히 버튼 전담이라 롱프레스 시 iOS 텍스트 선택 팝업이 뜰 수 있다 — 카드
영역엔 `-webkit-touch-callout:none` 유지, 본문 텍스트는 선택 가능해야 하므로 예외.

⚠ **iOS Safari `position:fixed;inset:0`은 주소창이 떠 있을 때도 큰 뷰포트 기준으로
높이를 잡는 경우가 있다**(2026-09-03 실기기 스샷에서 확인) — `#app`에 `height:100dvh`를
유지할 것. 단일화면 재설계 이후 재확인은 안 함.

### 하단 시트(모아보기) — 우측 레일 ☰로 열림

⚠ **진입점은 이거 하나뿐이다.** 예전엔 "오늘 카운터 꾹 누르면 열림"이라는 숨은 경로가
있었는데, 사용자가 그걸 몰라서 "캘린더가 안 보인다"는 버그로 오인한 적이 있어 없앴다
(2026-09-04).

- 항상 **캘린더가 기본**. 실제 날짜 그리드(일~토 정렬), 각 칸 좌상단에 날짜 숫자,
  중앙에 그날 본 개수(밝은 흰색, 0이면 숫자 자체를 안 그림 — 위 "코랄은 오늘 칸에만"
  참조). 배경 농도로 개수를 또 표현하지 않는다(숫자 하나로 충분하다는 판단). **최근 4주 고정 창** — 그 이전 기록은 캘린더에
  안 뜨고 검색/차수탭으로 찾는다(캘린더=최근 리듬, 검색/탭=전체 아카이브로 역할 분리.
  무한 과거 스크롤은 기각 — 부채감 없는 자유 탐색을 우선하는 방향과 같은 이유, 아래
  "메인 덱 공급 규칙" 참조).
- 날짜(오늘 포함)를 누르면 그 자리가 **그날 본 카드 제목 목록**으로 바뀐다(같은 자리
  토글, `← 캘린더`로 복귀). 오늘 칸은 0개여도 눌러서 빈 상태를 볼 수 있다.
- 검색 + 전체/0/1/2/3차 탭도 같은 자리(`#calArea`)를 공유 — 전체 DB 대상, 탭 재클릭하면
  해제돼 캘린더로 돌아간다.
- **목록에서 아무 항목이나 고르면 그 지점부터 순차 복습 세션이 시작된다**(맨 위=처음부터,
  다른 항목=거기서부터). 캘린더 날짜 클릭도 같은 세션 메커니즘을 쓴다.
- 시트를 아래로 스와이프하면 닫힌다(리스트/캘린더 스크롤이 맨 위일 때만 반응 — 스크롤
  중엔 안 뺏김. 손잡이 영역은 항상 반응).

✓ **캘린더의 "지난 날짜" 목록·개수는 실제 데이터다**(2026-09-04 이식 완료) — 서버가
`GET /api/calendar`(최근 N일 개수)·`GET /api/day?date=`(그날 채점된 카드 목록)로 내려준다.
위 "현재 상태" 섹션의 "캘린더가 실제 데이터를 쓴다" 참조.

### 메인 덱 공급 규칙 — 서버 쪽, 실조건검증 완료

`GET /api/cards`(`handleCards`/`pickByDate`, `src/worker.js`)가 끝없는 연습 큐를 채운다.

- **생성일자 오름차순(오래된 것부터)이다**(2026-09-04, 사용자가 Notion에서 쓰던 방식
  그대로 요청 — `완료도 0` 70%+복습 때 된 것 30% 가중치 뽑기와 정통 간격반복 기반 "때가
  됨" 경과일 로직(`1차 3일·2차 7일·3차 21일`)을 전부 폐기하고 대체했다). 클라이언트가
  마지막으로 받은 카드의 `created`를 `sd_cursor`(localStorage)에 저장해 `after` 쿼리로
  보내고, 서버는 그 지점 다음부터 이어서 `n`개를 준다 — 끝에 닿으면 처음으로 되감는다
  (고리형, 기존 덱 순환과 같은 정신). 그래서 다음 접속 때도 어디까지 봤는지 이어진다.
  ⚠ 이 순서는 완료도(step)·복습 상태와 완전히 무관하다 — 이미 3차까지 끝난 카드도 자기
  차례가 오면 그대로 다시 보여준다(의도된 동작. "구분 자체를 없앤다"가 사용자의 명시적
  요청이었음).
- ⚠ **인덱스 항목에 `created`가 있어야 정렬이 된다** — 이 필드는 2026-09-04에 추가돼서,
  그 이전에 동기화된 인덱스에는 없다. 새 배포 직후 `/api/sync?restart=1`로 강제 재동기화
  한 번 필요(그 시점에 이미 실행함 — `parserVersion`은 안 올렸으므로 카드 본문 재수집은
  없고, 행 목록만 다시 훑어 인덱스를 재구성한다. 버킷은 대부분 재사용돼 빠르다).
- **메인 게이지(완료도=0 잔량 표시)는 폐기했다**(2026-09-04) — 0차가 1,000장을 넘어가면
  숫자가 줄어드는 게 안 보여서 의미가 없어졌다. 그 자리는 위 "오늘 카운터 + 캘린더"가
  대신한다.

⚠ **iOS Safari에는 진동/햅틱 API가 없다**(iOS 26.5에서 `<input switch>` 우회도 막힘).
보상 피드백은 색·모션·소리로만 설계한다(2026-09-04: 완료 버튼 펄스·세션종료카드가 이
역할). 햅틱이 꼭 필요해지면 그때 Capacitor로 감싼다(Apple Developer $99/년 — 그 전엔
낼 이유 없음).

## 배포

**https://shorts-deck.shorts-deck.workers.dev**
(Cloudflare 계정 `7maker@gmail.com`, KV `929a2e986f2447689d4bb936327e6120`)

- 2026-09-03: 저장 개편(1,323/1,323 동기화) + 구버전 UI(1단/2단 카드 모델) 최초 배포.
- 2026-09-04: 새 UX(1단 폐지·단일화면·캘린더) 전체 이식 배포. `Version ID
  0d422c35-de55-4d8a-817b-7ad2300c1775`.
- 2026-09-05: 챕터/시험 재설계 배포. `Version ID e9d4ff0c-b8c1-4a47-957b-04b46c413c59`.
  로컬 `wrangler dev`(실제 Notion 데이터 일부를 로컬 전용 미니 KV로) + Playwright로
  챕터개요·챕터상세·학습·시험·제목고치기·형광펜까지 전 화면 클릭 검증 후 배포. 배포
  직후 `/api/sync?restart=1`로 전량 재동기화 시작(`PARSER_VERSION` 3 — 블록에 원본
  Notion block id를 채워야 형광펜이 Notion에 반영됨). 기존 `grades`/`daily:v1:*` KV는
  새 코드가 안 읽어 그냥 방치(무해, 실사용 데이터가 거의 없어 초기화 부담도 낮았음).

새 PC/새 계정에서 처음 세울 때:

```
npx wrangler kv namespace create KV     # 나온 id를 wrangler.toml에 기입
npx wrangler secret put NOTION_TOKEN    # 크롬 확장이 쓰는 것과 같은 integration 토큰
npx wrangler deploy
```

로컬 개발은 `.dev.vars`에 `NOTION_TOKEN=...` (gitignore됨) 후 `npm run dev`.
카드를 즉시 다시 긁으려면 `/api/sync?restart=1`을 `done:true`가 나올 때까지 호출.

⚠ 그 integration이 대상 DB에 **연결**돼 있어야 한다. 안 되어 있으면 404가 뜬다.

⚠ 신규 `*.workers.dev` 서브도메인은 TLS 인증서 발급에 수 분 걸린다 — 그 사이
`ERR_SSL_VERSION_OR_CIPHER_MISMATCH`가 뜨는 건 정상이고 기다리면 된다.

⚠ 이 PC의 Windows curl(schannel)은 `workers.dev`에 간헐적으로 붙지 못한다 — TLS 핸드셰이크
실패(`HTTP 000`)뿐 아니라 **연결은 되고 응답만 무한 대기하는 형태로도** 나타난다(2026-09-03
실측: 같은 순간 `node -e "fetch(...)"`와 브라우저는 정상 200). Worker 장애나 `503` 재발로
오해하기 딱 좋다 — **검증은 브라우저나 node `fetch`로 할 것. curl 하나로 판단하지 말 것.**

## 파일

```
src/worker.js            API + 정적 서빙. Notion 접근은 전부 여기
wrangler.toml            Worker 설정. KV id는 배포 시 채울 것
package.json             npm run dev / deploy / kv:create
public/index.html        앱 본체 (좌우 스와이프=이동·완료 버튼=채점·하단 시트 캘린더+모아보기). 블록 렌더는 여기
public/manifest.json     홈 화면 웹앱 메타 (standalone·세로 고정)
public/cards.seed.json   폴백 씨앗 21장. ⚠ 사본을 만들지 말 것 — 여기가 유일
prototype.html           Artifact 게시용 스냅샷. 앱과 별개, 동기화 안 됨
tools/build_cards.py     초기 1회 스크래퍼. 입력 파일은 repo에 없어 재실행 불가
```

⚠ `public/cards.seed.json`을 다른 폴더에 복사해 두지 말 것 — 한때 `data/`에 같은
사본이 있었고, 둘을 맞춰 줄 장치가 없어 어느 쪽이 진짜인지 알 수 없는 상태가 됐다.
씨앗을 다시 만들 일이 생기면 `tools/build_cards.py`가 `public/`에 직접 쓴다.
